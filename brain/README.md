# brain

Analysis package that runs outside Katana: wraps COLMAP for 3D tracking, Grounded SAM 2 for light detection, then fuses the results and writes a USDLux `.usd` file plus JSON sidecar for the validation app.
