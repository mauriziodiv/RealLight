# brain/fuse

Triangulates detection masks to 3D positions, rejects reflections by cross-view consistency (real emitters triangulate to a stable point; reflections don't), and classifies each candidate's light type.
